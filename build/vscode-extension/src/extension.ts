import type * as vscode from 'vscode'

export function activate(_context: vscode.ExtensionContext) {
  // The MCP server configuration is contributed via package.json
}

export function deactivate() {}
